import { defineConfig } from 'astro/config';

// Repositório: jcpersonaltr-prog.github.io (site de utilizador -> vive na raiz).
// Quando tiveres domínio próprio, atualiza apenas o valor de "site".
export default defineConfig({
  site: 'https://jcpersonaltr-prog.github.io',
});
